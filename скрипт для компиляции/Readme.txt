Перед тем, как использовать убедись,
что у тебя стоит Python 3+ и cx_freeze(cmd>> pip3 install cx_freeze)

Before using make sure to install 
Python 3+ and cx_freeze as well(cmd>> pip3 install install cx_freeze)
